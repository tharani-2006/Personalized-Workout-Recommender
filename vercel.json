
{
  "version": 2,
  "builds": [
    { "src": "api/predict.py", "use": "@vercel/python" },
    { "src": "index.html", "use": "@vercel/static-build", "dest": "." }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

